/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname:"madhack.ucscieee.lk",protocol:"https"}
        ]
    }
};

export default nextConfig;
